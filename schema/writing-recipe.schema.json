{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "WritingRecipe",
  "type": "object",
  "required": [
    "key",
    "version",
    "name",
    "required_moves",
    "planner_rules",
    "audit_rules"
  ],
  "properties": {
    "key": {
      "type": "string"
    },
    "version": {
      "type": "string"
    },
    "name": {
      "type": "string"
    },
    "description": {
      "type": "string"
    },
    "project_id": {
      "description": "null이면 공용 기본 방식, 문자열이면 해당 프로젝트 소유 방식",
      "type": [
        "string",
        "null"
      ]
    },
    "best_for": {
      "type": "string"
    },
    "pattern_preview": {
      "readOnly": true,
      "description": "required_moves의 화면 표시명을 서버가 자동 생성한 호환 필드",
      "type": "array",
      "minItems": 3,
      "items": {
        "type": "string"
      }
    },
    "moves": {
      "readOnly": true,
      "description": "각 문단 방식의 고정 목적을 서버가 자동 생성한 호환 필드",
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": [
          "id",
          "purpose"
        ],
        "properties": {
          "id": {
            "type": "string"
          },
          "purpose": {
            "type": "string"
          }
        }
      }
    },
    "required_moves": {
      "description": "글의 흐름과 감사에서 강제하는 실제 문단 방식 순서",
      "type": "array",
      "minItems": 3,
      "items": {
        "type": "string"
      }
    },
    "optional_moves": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "planner_rules": {
      "type": "array",
      "items": {
        "type": [
          "string",
          "object"
        ]
      }
    },
    "style_defaults": {
      "type": "object"
    },
    "audit_rules": {
      "type": "array",
      "items": {
        "type": "string"
      }
    }
  }
}

{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "PlaybookSession",
  "type": "object",
  "required": [
    "project_id",
    "concept_slots",
    "writing_recipe_id",
    "settings_json"
  ],
  "properties": {
    "project_id": {
      "type": "string"
    },
    "concept_slots": {
      "type": "object",
      "properties": {
        "subject": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "background": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "elements": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "conflicts": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "wildcards": {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      }
    },
    "direction_card_ids": {
      "description": "현재 프로젝트에 저장된 집필 지침(DirectionCard) ID 목록",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "user_direction": {
      "description": "이번 초안에만 적용하는 추가 지시",
      "type": "string"
    },
    "writing_recipe_id": {
      "description": "프로젝트와 무관한 공유 전개 방식(WritingRecipe) ID",
      "type": "string"
    },
    "output_profile": {
      "description": "결과물 형태(Output Profile) key",
      "type": "string"
    },
    "settings_json": {
      "type": "object",
      "properties": {
        "length": {
          "enum": [
            "short",
            "normal",
            "long",
            "very_long",
            "custom"
          ]
        },
        "detail_level": {
          "type": "integer",
          "minimum": 1,
          "maximum": 5
        },
        "context_depth": {
          "enum": [
            "core",
            "balanced",
            "wide",
            "max"
          ]
        },
        "creativity": {
          "enum": [
            "strict",
            "conservative",
            "balanced",
            "free"
          ]
        },
        "viewpoint": {
          "enum": [
            "omniscient",
            "first_observer",
            "third_limited"
          ]
        },
        "tense": {
          "enum": [
            "present",
            "past"
          ]
        },
        "mystery_preservation": {
          "type": "integer",
          "minimum": 1,
          "maximum": 5
        }
      }
    },
    "seed": {
      "type": "integer"
    }
  }
}

# AGENTS.md — Lore Studio 구현 규칙

이 파일은 Codex나 다른 코딩 에이전트가 프로젝트의 철학을 깨뜨리지 않고 작업하도록 하는 구현 계약입니다.

## 불변 설계 규칙

1. **컨셉 페이지, 방향성 카드, 집필 레시피, 출력 프로필을 하나의 프롬프트 필드로 합치지 않는다.**
2. 생성된 내용은 `CANDIDATE` 상태로만 제안하며, 사용자 승인 없이 `PROJECT_CANON`으로 승격하지 않는다.
3. 컨셉 페이지는 자유 본문이 중심이며, 구조화 속성은 보조 수단이다. 빈 필드를 강제하지 않는다.
4. 다른 IP의 내용과 작문 참고 표본은 사용 역할을 구분한다. 작문 참고 자료의 사실이 프로젝트 설정으로 유입되지 않게 한다.
5. 플레이북은 채팅 화면이 아니라 재현 가능한 생성 실행 단위다. 입력 선택, 시드, 모델, 레시피 버전, 출력 설정을 기록한다.
6. 장문 생성 시 모든 원문을 무작정 붙이지 않는다. 컨텍스트 컴파일러가 핵심 사실·요약·관련 원문·열린 질문·금지 설정을 분리한다.
7. 생성 오류를 다음 생성의 사실로 증폭시키지 않는다. 사용자 승인 경계를 보존한다.
8. 집필 레시피는 소재별 목차가 아니라 정보 공개 순서와 수사적 이동을 정의한다.
9. 모델 런타임은 OpenAI 호환 게이트웨이 뒤에 숨긴다. 특정 llama.cpp/vLLM 구현을 도메인 코드에 직접 결합하지 않는다.
10. 신규 기능을 추가할 때 `docs/`, JSON Schema, API 스키마, 테스트를 함께 갱신한다.
11. **글 만들기의 하단 `이전 / 계속` 이동 바는 본문 스크롤 영역 밖에 둔다.** `확인·작성`을 포함한 모든 선택 단계에서 데스크톱 작업 화면의 고정 행, 모바일 하단 전역 메뉴 바로 위의 고정 행으로 유지하며 카드·폼·검토 내용과 함께 스크롤되게 만들지 않는다.

## 코드 작업 규칙

- 백엔드: Python 3.11 이상, FastAPI, SQLAlchemy 2, Pydantic 2.
- 프론트엔드: SvelteKit, Tiptap. 서버 API 호출은 `src/lib/api.js`를 경유한다.
- 데이터베이스: PostgreSQL이 권위 저장소다. JSON 필드는 유연성을 위해 사용하되 핵심 식별자와 상태는 정규 컬럼으로 둔다.
- 모든 생성 실행은 `GenerationRun`에 모델, 프롬프트 구성요소, 입력 선택, 파라미터를 기록한다.
- 파괴적 데이터 변경은 명시적 사용자 액션과 감사 기록을 요구한다.
- 테스트는 최소한 컨텍스트 컴파일, 상태 승격 금지, 레시피 로딩을 포함한다.

## 우선순위 판단

기능을 추가할 때 다음 순서를 우선한다.

1. 사용자가 설정을 편하게 축적할 수 있는가
2. 사용자가 생성 입력을 이해하고 통제할 수 있는가
3. 결과를 부분 수정하고 다시 사용할 수 있는가
4. 생성 과정이 재현 가능하고 추적 가능한가
5. 자동화가 사용자의 설정 권한을 침해하지 않는가

화려한 에이전트 자율성, 그래프 시각화, 자동 세계관 생성은 위 기반보다 뒤에 둔다.
